language: node_js
node_js:
- 10

services:
- docker

sudo: required

branches:
  only:
  - master
  - gh-pages

install:
- npm install
- ./scripts/docker_install_run.sh

before_script:
- sleep 20
- docker ps -a

script:
- npm run test

cache:
  npm: true
  directories:
  - ~/.cache

env:
  global:
  - MOZ_HEADLESS=1
addons:
  firefox: latest