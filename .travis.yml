language: node_js
node_js:
- 10

branches:
  only:
  - master

script:
- ./scripts/docker_install_run.sh
- npm install
- npm run test

cache:
  npm: true
  directories:
  - ~/.cache

env:
  global:
  - MOZ_HEADLESS=1
addons:
  firefox: latest